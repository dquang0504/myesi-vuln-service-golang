kafka_broker: "kafka:9092"
kafka_topic: "vuln.refresh"
consumer_group: "vuln-consumer"

scheduler_spec: "@every 6h"
scheduler_batch_limit: 50

repo_cleanup_spec: "@daily"         # daily repo stale check
repo_retention_days: 7              # 7 days stale => delete repo